# ТЗ 1.2 — приведение калькуляции к реальной модели Excel (вариант А)

> Проект: NVGRN Hub / Novagreen Foods
> Основание: разбор файла `00calc_NVGRN11.xlsx` (11 листов, все формулы выверены по ячейкам)
> Дополняет: `TZ_CALCULATION.md` версии 1.1
> Статус: **на утверждение Шоху**. До подтверждения денежные формулы не меняем.
> Дата: 9 августа 2026

---

## 1. Зачем этот документ

Этапы 1–3 уже работают: расчётное ядро, изделия и рецептуры, матрица, периоды,
утверждение версий, история. Формулы были написаны по тексту ТЗ 1.0/1.1 и покрыты
тестами, но с настоящим Excel Шоха они **не сверялись**.

Разбор Excel показал 7 расхождений в формулах и несколько проблем в данных.
Этот документ фиксирует, что именно меняем, чтобы система считала правильно и при
этом её цифры можно было объяснить рядом с привычной таблицей.

**Принятое решение — вариант А:** считаем математически правильно, а расхождения с
Excel показываем отчётом по каждому изделию. Excel не является эталоном там, где он
арифметически неверен; он является эталоном структуры и бизнес-смысла.

---

## 2. Как считает Excel (выверено по ячейкам)

Все семь расчётных листов используют одну цепочку. Пример — латук 100 г, лист `рознич.тара`:

```
зелень          = 17 000 / 1000 × 100 г            = 1 700,00
упаковка        = комплект «вак.пакет»             = 1 007,25
ФОТ             = 263 613 636 / 55 000             = 4 792,98
прочие произв.  = 15 980 800 / 55 000              =   290,56
накладные       = 22 539 156 / 55 000              =   409,80
                                            с/с   = 8 200,59
с/с с браком    = с/с × (1 + 0,5)                  = 12 300,88
ретро           = 21 000 × 21%                     = 4 410,00
НДС             = 21 000 × 12%                     = 2 520,00
прибыль         = 21 000 − 12 300,88 − 4 410 − 2 520 = 1 769,12
налог           = 1 769,12 × 15%                   =   265,37
чистая прибыль                                     = 1 503,75
чистая маржа    = 1 503,75 / 21 000                =     7,16 %
```

Источники: `зелень-"сырье"` (колонка E — вручную выбранный месяц из 30+ колонок),
`Упаковка` (комплекты: вак.пакет 1007,25 = пакет 657,25 + самоклейка 300 + прочие 50),
`ФОТ` (оклады × коэффициент налогов), `Произодство` (выпуск 55 000 шт, две группы расходов).

---

## 3. Изменения формул

### 3.1. НДС — оставляем правильный расчёт (изменений в коде нет)

| | Формула | Латук 21 000 |
|---|---|---|
| Excel | `НДС = цена × 12%`, затем вычитается из цены | 2 520 |
| Система | цена включает НДС → `НДС = цена × 12 / 112` | **2 250** |

Если 21 000 уже включает НДС, налог внутри цены равен 2 250. В Excel здесь
арифметическая неточность: 270 сум с единицы, маржа занижена примерно на 1 п.п.
(Excel 7,16 % против 8,25 %).

**Решение:** считаем правильно. Расхождение показываем в отчёте сверки отдельной строкой,
чтобы Шох видел, что маржа выросла из-за исправления счёта, а не из-за изменений в бизнесе.

### 3.2. Технологические потери — оставляем делитель, проценты конвертируем

| | Формула | Латук 500 г, «отход» 50 % |
|---|---|---|
| Excel (`хорека500`) | `(цена + цена × отход) / 1000 × граммы` | 25 500 / 1000 × 500 = 12 750 |
| Система | `кол-во / (1 − потери)` × цена | 0,5 / 0,5 × 17 000 = 17 000 |

«+50 % к цене» и «выход 50 %» — разные вещи. Правильный смысл: если из килограмма сырья
получается 500 г продукта, нужно **два** килограмма.

**Решение:** формулу не меняем. При переносе конвертируем проценты Excel:

```
потери_системы = отход_Excel / (1 + отход_Excel)
```

| Отход в Excel | Потери в системе |
|---|---|
| 0,2 (20 %) | 16,67 % |
| 0,5 (50 %) | 33,33 % |
| 0,9 (90 %) | 47,37 % |
| 1,2 (120 %) | 54,55 % |

Конвертация даёт ровно ту же стоимость строки, что в Excel. Таблицу конвертации приложить
к отчёту сверки.

### 3.3. Резерв брака — добавить переопределение на уровне изделия

В Excel ставка резерва разная не только по листам, но и по отдельным товарам, причём
вписана прямо в формулу ячейки:

| Где | Ставка |
|---|---|
| `торг` | 3 % |
| `пучки и горшки` | 15 % |
| `рознич.тара`, `салаты`, `хорека250г`, `хорека500` | 50 % |
| мангольд 100 г (`I14 = I13*20%+I13`) | **20 %** |
| руккола 200 г (`M14 = M13*$N$13+M13`) | **5 %** |
| стебель сельдерея (`M14 = M13*35%+M13`) | **35 %** |
| романо-цезарь, шпинат мороженный (`F16 = F15`) | **0 %** |

Сейчас резерв задаётся только на группе (так требовало ТЗ 1.1). Этого недостаточно.

**Решение:** резерв группы остаётся значением по умолчанию, у изделия появляется
**необязательное переопределение**. Правило применяет сервер:

```
резерв = переопределение_изделия, если задано; иначе резерв_группы
```

В интерфейсе переопределение подписывается явно: «резерв группы 50 %, у этого изделия 20 %».

### 3.4. Все накладные — до резерва брака

| | Порядок |
|---|---|
| Excel | все накладные внутри с/с, затем `× (1 + резерв)` |
| Система сейчас | `(сырьё + упаковка + ФОТ + производственные + админ) × (1 + резерв)` **+** коммерческие + логистика + финансы |

При резерве 50 % расхождение равно `(коммерческие + логистика + финансы) × 0,5`.

**Решение:** приводим к Excel. Новая цепочка слоёв:

```
Себестоимость до резерва =
    сырьё + упаковка + ФОТ_на_ед + производственные_на_ед
  + административные_на_ед + коммерческие_на_ед + логистика_на_ед + финансовые_на_ед

Полная себестоимость = Себестоимость до резерва × (1 + резерв / 100)
```

Пять блоков расходов **по-прежнему показываются отдельными строками** матрицы — меняется
только момент применения резерва. Это изменение денежной формулы, поэтому версия формул
поднимается до `v2`; ранее утверждённые снимки остаются на `v1` и не пересчитываются.

### 3.5. Доля производственных затрат — новое явное поле

В Excel есть невидимые делители:

| Где | В формуле | Смысл |
|---|---|---|
| `торг` (весь лист) | `ФОТ / 55000 / 5`, подписано «ФОТ 5-ая часть» | пятая часть |
| `пучки и горшки` | `ФОТ / 70000 / 2` | другой знаменатель + половина |
| руккола 100 г | `SUM(ФОТ; прочие) / 2` | половина |
| руккола 200 г | `ФОТ / 55000 / 2`, затем ещё `/2` | четверть ФОТ и половина прочих |

По сути это **коэффициент трудоёмкости**: на пучок или на овощи тратится меньше труда, чем
на фасовку салата. ТЗ 1.0 (§13.5) запрещает такие скрытые делители — коэффициент должен
иметь название, значение и объяснение.

**Решение:** у изделия появляется поле **«доля производственных затрат»**
(по умолчанию 1). Оно умножает ФОТ на единицу и производственные расходы на единицу:

```
ФОТ_на_ед            = (Полная нагрузка ФОТ / Общий выпуск) × доля
производственные_на_ед = (Производственные месяца / Общий выпуск) × доля
```

Накладные (админ, коммерческие, логистика, финансы) на долю **не** умножаются — они
общехозяйственные и не зависят от трудоёмкости.

Значения для переноса: `торг` → 0,2; пучки и горшки → 0,3928 (учитывает и знаменатель
70 000, и делитель 2); руккола 100 г → 0,5. По рукколе 200 г в Excel ФОТ делится дважды —
это похоже на ошибку, поэтому предлагаю 0,5 и отдельно спрашиваем Шоха (см. §11).

Поле закрывает и вопрос разных знаменателей: общий выпуск остаётся один (сумма плана),
а разница трудоёмкости выражается долей.

### 3.6. Налоги ФОТ — берём факт, показываем сравнение с коэффициентом

Excel считает налоги коэффициентом: `оклады / 0,88 × 1,24` (≈ +40,9 %) для администрации и
производства и `/ 0,88 × 1,12` (≈ +27,3 %) для третьего блока.

Система берёт **фактические** ИНПС + НДФЛ + соцналог из «Кадров» — это правильнее.

**Решение:** источник не меняем. В панели периода дополнительно показываем строку
«налоги ФОТ составляют N % от начислено» — чтобы Шох сразу видел отклонение от привычных
40,9 % и понимал причину расхождения с Excel.

### 3.7. Знаменатель — остаётся сумма планового выпуска

Excel использует фиксированные 55 000 (и 70 000 для пучков). Система суммирует план по
изделиям. После §3.5 разные знаменатели больше не нужны.

**Решение:** оставляем сумму плана. В отчёте сверки фиксируем, что для совпадения с Excel
сумма плана должна быть 55 000.

---

## 4. Изменения модели данных

Только добавочные и идемпотентные операции. Существующие данные не удаляются.

| Таблица | Что добавляем | Зачем |
|---|---|---|
| `calculation_products` | `waste_reserve_override NUMERIC` (nullable) | резерв брака у отдельного изделия (§3.3) |
| `calculation_products` | `production_share NUMERIC NOT NULL DEFAULT 1` | доля производственных затрат (§3.5) |

Существующая колонка `waste_reserve_rate` в `calculation_products` остаётся для
обратной совместимости и в новом расчёте не используется: `NULL` в новой колонке нужен,
чтобы отличать «не задано» от «задано 0 %», а старая колонка этого не позволяет.

Версия формул в снимках: новые расчёты пишут `v2`, старые снимки остаются `v1`.

---

## 5. Изменения интерфейса

1. **Карточка изделия** — два новых необязательных поля в отдельном блоке
   «Особые условия изделия»:
   - «Резерв брака, %» с подписью «по умолчанию берётся из группы: 50 %»;
   - «Доля производственных затрат» с подписью «1 = обычная фасовка, 0,5 = вдвое меньше труда».
   Обычная карточка остаётся короткой: блок свёрнут, пока значения не заданы.
2. **Матрица** — в строке «Резерв брака, %» показывать пометку «своё» у изделий с
   переопределением; новая строка «Доля производственных затрат» в подробном виде.
3. **Панель периода** — строка «налоги ФОТ = N % от начислено» (§3.6) и блок
   «компоненты без закупочной цены» со ссылкой в Закуп.
4. **Отчёт сверки с Excel** — отдельная кнопка на вкладке «Калькуляция»: таблица
   «показатель / Excel / система / разница / причина» по выбранным контрольным изделиям.
   Данные Excel вводятся один раз как эталонный набор (см. §7).

---

## 6. Перенос данных из Excel

### 6.1. Группы товаров

Из листов Excel получается 6 групп. Ставки взяты из ячеек, а не «на глаз»:

| Группа | Лист | Ретро | Резерв брака | НДС |
|---|---|---|---|---|
| Розница | `рознич.тара` | 21 % | 50 % | 12 % |
| Салаты розница | `салаты` | **23 %** | 50 % | 12 % |
| HoReCa 250 г | `хорека250г` | 0 % | 50 % | 12 % |
| HoReCa 500 г | `хорека500` | 0 % | 50 % | 12 % |
| Пучки | `пучки и горшки` | 0 % | 15 % | 12 % |
| Горшки | `пучки и горшки`, блок Q–S | **11 %** | 0 % | 12 % |
| Овощи и прочее | `торг` | 0 % | 3 % | 12 % |

Обрати внимание: у розницы и салатов **разное ретро** (21 % и 23 %), поэтому это две группы,
а не одна. Горшки отличаются от пучков и ретро, и резервом.

### 6.2. Комплекты упаковки групп

| Комплект | Состав | Итого |
|---|---|---|
| вак.пакет | вакуумный пакет 657,25 + самоклейка 2 стороны 300 + прочие материалы 50 | 1 007,25 |
| хорека | одна позиция | 600 |
| плёнка цветная | плёнка 464,4 + стикер 120 | 584,40 |
| плёнка без стикера | плёнка 464,4 | 464,40 |
| хорека вак | одна позиция | 1 300 |
| бокс для лагмана / маставы | одна позиция | 3 000 / 4 300 |

Все элементы должны существовать в `ref_packaging`. Цену система возьмёт из Закупа;
если у элемента нет приёмки — строка честно покажет «нет закупочной цены».

### 6.3. Сырьё и изделия

- 30 позиций зелени должны быть в `ref_raw_materials`. Цены система берёт из Закупа
  (последняя принятая), а не из ручной колонки Excel.
- Салаты и смеси в Excel заданы **процентами содержания**, а рецептура системы хранит
  граммы: `граммы = граммовка × процент / 100`. Пересчёт выполняем при переносе.
- Салаты в Excel ссылаются на полуфабрикат «смесь» одной строкой. В системе состав
  раскрывается сразу в сырьё (плоская рецептура). Вложенные полуфабрикаты — вне этой версии.

---

## 7. Методика сверки (главное)

Чтобы не путать расхождение формул с расхождением цен, сверка идёт в два шага.

**Шаг 1 — сверка формул при одинаковых ценах.** Расчёт запускается в режиме модели, где
цены сырья и упаковки принудительно равны колонке E листа `зелень-"сырье"` и комплектам
листа `Упаковка`. ФОТ, выпуск и расходы берутся из листов `ФОТ` и `Произодство`.
На этом шаге любое расхождение — это **разница формул**, и она обязана объясняться
пунктами §3.1–3.5.

**Шаг 2 — сверка с живыми ценами.** Тот же расчёт на ценах из Закупа. Здесь расхождение
объясняется тем, что Excel брал цену вручную выбранного месяца, а система — последнюю
принятую приёмку. Это ожидаемо и правильно.

**Контрольные изделия** (ТЗ 1.0 §25.3):

| Изделие | Лист | Что проверяет |
|---|---|---|
| Латук 100 г | `рознич.тара` | монопродукт, ретро 21 %, резерв 50 % |
| Латук 500 г | `хорека500` | технологические потери (§3.2) |
| Мангольд 100 г | `рознич.тара` | резерв 20 % у отдельного изделия (§3.3) |
| Салат к мясу (Noemy) 150 г | `салаты` + `сост_салаты` | многокомпонентная смесь, ретро 23 % |
| Укроп пучок 40 г | `пучки и горшки` | доля производственных затрат (§3.5) |
| Броколли 1 кг | `торг` | резерв 3 %, доля 0,2 |

Результат — таблица по каждой строке расчёта: `показатель / Excel / система / разница /
причина`. Каждая ненулевая разница обязана иметь причину из §3. Разница без объяснения
считается ошибкой и правится до сдачи.

---

## 8. Тесты

К существующим 64 тестам добавляются:

1. Резерв изделия перекрывает резерв группы; при `NULL` берётся группа; `0` у изделия —
   это именно ноль, а не «не задано».
2. Все пять блоков накладных умножаются на резерв (§3.4): проверка на числах, где
   старый и новый порядок дают заметно разный итог.
3. Доля производственных затрат умножает ФОТ и производственные, но **не** трогает
   административные, коммерческие, логистику и финансы.
4. Конвертация потерь: `отход_Excel / (1 + отход_Excel)` даёт ту же стоимость строки, что
   формула Excel `(цена + цена × отход)`. Проверить на 0,2 / 0,5 / 0,9 / 1,2.
5. Контрольный расчёт латука 100 г при ценах Excel: полная себестоимость 12 300,88 и
   НДС 2 250 (не 2 520) — фиксируем ожидаемое расхождение как осознанное.
6. Версия формул: новый расчёт возвращает `v2`, а снимок с `v1` не меняется.

---

## 9. Этапы

Каждый этап — отдельный протестированный локальный коммит. Push — по команде Шоха.

**Этап 4а. Формулы и поля.** Схема (`waste_reserve_override`, `production_share`),
двигатель `v2` (накладные до резерва, доля производственных, резерв изделия), тесты §8,
поля в карточке изделия и строки в матрице.
*Результат: система считает по выверенной модели.*

**Этап 4б. Перенос справочных данных.** Шесть групп с ставками из §6.1, комплекты
упаковки из §6.2. Идемпотентно, существующие записи не ломаем.
*Результат: рабочие «листы» повторяют Excel.*

**Этап 4в. Сверка.** Загрузка эталонных значений Excel по шести контрольным изделиям,
отчёт «Excel / система / разница / причина», таблица конвертации потерь.
*Результат: подтверждённая правильность расчёта, расхождения объяснены.*

Далее по основному ТЗ: этап 5 — модели, этап 6 — факт месяца, этап 7 — уведомления и
сценарий дефицита, этап 8 — Telegram-бот.

---

## 10. Критерии приёмки

1. Резерв брака можно задать у отдельного изделия, и он перекрывает группу.
2. Доля производственных затрат видна как поле с объяснением; скрытых делителей нет.
3. Все накладные умножаются на резерв брака, порядок слоёв совпадает с Excel.
4. По шести контрольным изделиям расхождение с Excel либо ноль, либо объяснено
   пунктом §3 с указанием суммы.
5. Расхождение по НДС показано отдельной строкой и подписано как исправление счёта.
6. Технологические потери переносятся по таблице конвертации и дают стоимость строки
   Excel до копейки.
7. Отсутствие цены компонента не превращается в ноль и блокирует утверждение.
8. Отрицательный налог на прибыль не создаётся (в Excel он есть — у розмарина 250 г).
9. Ранее утверждённые снимки не изменились и остались на версии формул `v1`.
10. `npm run verify` проходит полностью.

---

## 11. Открытые вопросы к Шоху

Работа над этапом 4а может начаться и без ответов — вопросы касаются только переноса
конкретных цифр. Но до сдачи сверки ответы нужны.

1. **Руккола 200 г.** В Excel ФОТ делится дважды (`/2` в строке ФОТ и ещё `/2` в строке
   производственных затрат), то есть фактически на 4, а прочие — на 2. Похоже на ошибку.
   Предлагаю единую долю 0,5. Подтвердить или назвать верное значение.
2. **Компоненты с нулевой ценой.** В `зелень-"сырье"` цена 0 у: черри, кунжут, соусы
   ореховый и гранатовый, красная капуста, сухарики, капуста белокочанная. В салатах они
   есть в составе, но стоят 0 — салаты недооценены. Нужны реальные цены (тогда себестоимость
   салатов вырастет) или подтверждение, что компонент бесплатный (например, идёт из отходов —
   такая пометка в Excel есть у моркови и капусты).
3. **Проценты состава салатов.** У «Салат к мясу (Noemy)» сумма долей 138,3 %, а
   проверочная строка складывает только первые четыре компонента и показывает 100 %.
   Нужно уточнить фактический состав: система потребует, чтобы граммы сходились с граммовкой.
4. **Стебель сельдерея 500.** В формуле `цена / 35 × граммы` вместо `/1000`. Что означает 35 —
   вес одного стебля, количество в упаковке? Нужно название и смысл, иначе это скрытый коэффициент.
5. **Романо-цезарь и шпинат мороженный.** Резерв брака отключён полностью (`F16 = F15`).
   Это осознанно или потерянная формула?
6. **Разные месяцы цен.** В колонке «Цены в кальк» у разных позиций выбраны разные месяцы
   (латук — июнь 2026, мангольд — июль 2026, морковь — ноябрь 2025). После переезда цена
   всегда будет последней принятой из Закупа. Подтвердить, что это устраивает.

---

## 12. Что НЕ входит в эту версию

- Вложенные полуфабрикаты (смесь как компонент другого изделия) — рецептура остаётся плоской.
- Автоматический импорт листов Excel: переносим один раз руками по §6, чтобы не тащить
  ошибки и скрытые коэффициенты.
- Коэффициенты трудоёмкости по операциям и сменам: только одно поле «доля» на изделие.
- Изменение источника ФОТ (остаются «Кадры») и источника расходов (остаётся «Касса»).
- Пересчёт ранее утверждённых версий.
